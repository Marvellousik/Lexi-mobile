# Set to 'true' to intercept API calls and return fixture data.
# Set to 'false' (or remove) to hit the real backend.
EXPO_PUBLIC_API_MOCK=true

# Real backend base URL (already configured in src/services/api.ts)
# EXPO_PUBLIC_API_URL=https://api.lexiassist.com/api/v1
